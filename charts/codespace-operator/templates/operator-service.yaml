{{- if and .Values.operator.metrics.enabled (ne .Values.operator.metrics.bindAddress "0") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "codespace-operator.metrics.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "codespace-operator.labels" . | indent 4 }}
  annotations:
{{ toYaml .Values.operator.metrics.service.annotations | indent 4 }}
spec:
  type: {{ .Values.operator.metrics.service.type }}
  selector:
{{ include "codespace-operator.selectorLabels" (dict "name" (include "codespace-operator.name" .) "root" .) | indent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.operator.metrics.service.port }}
      targetPort: {{ .Values.operator.metrics.service.targetPort }}
{{- end }}
